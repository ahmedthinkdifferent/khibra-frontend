import Layout from "../components/layout";

const profile = () => {
  return (
    <Layout pageTitle="profile">
      <h1 style={{ textAlign: "center", marginTop: "100px" }}>
        University profile is under development
      </h1>
    </Layout>
  );
};

export default profile;
